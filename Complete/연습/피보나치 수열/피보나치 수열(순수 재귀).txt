#include <bits/stdc++.h>
using namespace std;

// 재귀 피보나치 수열
// n이 커질 수록 성능이 떨어진다.
// 메모이제이션 X, 재귀 오버헤드
int Fibo(int n)
{
    if (n <= 1) return n;

    return Fibo(n - 1) + Fibo(n - 2);
}

int main(int argc, char* argv[])
{
    int n = 10;

    cout << Fibo(n);
}