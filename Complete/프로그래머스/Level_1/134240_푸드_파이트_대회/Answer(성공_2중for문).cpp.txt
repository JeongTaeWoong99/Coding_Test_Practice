#include <bits/stdc++.h>
using namespace std;

// food를 절반씩 나눠 좌우 대칭 형태의 대진표 문자열로 반환 (2중 for문으로 한 글자씩 채우기)
string solution(vector<int> food)
{
    string left = ""; // 왼쪽 절반 (물 제외)

    // 1번 음식부터 순서대로 (개수 / 2)번씩 한 글자씩 채우기
    for (int i = 1; i < (int)food.size(); i++)
    {
        char foodChar = '0' + i; // i번 음식을 나타내는 문자

        for (int j = 0; j < food[i] / 2; j++)
        {
            left += foodChar;
        }
    }

    string mid = "0"; // 가운데 물 (food[0]은 항상 1이므로 고정값 사용)

    string right = left; // 오른쪽 절반 (왼쪽을 뒤집어서 대칭)
    reverse(right.begin(), right.end());

    return left + mid + right;
}
